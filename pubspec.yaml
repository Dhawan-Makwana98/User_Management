name: user_management_demo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensure this range is compatible with your dependencies

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.25.4
  cloud_firestore: ^4.13.5
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  connectivity_plus: ^6.0.3
  uuid: ^4.3.3
  fluttertoast: ^8.2.12
  intl: ^0.20.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
